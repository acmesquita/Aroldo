<%= form_for(quadra) do |f| %>
  <% if quadra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quadra.errors.count, "error") %> prohibited this quadra from being saved:</h2>

      <ul>
      <% quadra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao %>
  </div>

  <div class="field">
    <%= f.label :geom %>
    <%= f.text_field :geom %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
